import styled from "styled-components";

export const Button = styled.button`
  display: grid;
  place-items: center;
  padding: 4px;
  font-size: 1.5rem;
  background: none;
  border: none;
  color: #009EDD;
  cursor: pointer;
`
